import React from 'react';

// Introduced duplicate default exports for Home component
const Home = () => {
    return (
        <div>
            <h1>Home Page</h1>
        </div>
    );
};

export default Home;